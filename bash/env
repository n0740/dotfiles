# PATH
# Initial PATH on OS X: /usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin
export PATH="/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin"
[ -d "$HOME/.bin" ] && export PATH="$HOME/.bin:$PATH"
# __unshift_to_path_if_exists = "$HOME/.bin"

# Locale
export LANG="en_US.UTF-8"
export LC_COLLATE="en_US.UTF-8"
export LC_CTYPE="en_US.UTF-8"
export LC_MESSAGES="en_US.UTF-8"
export LC_MONETARY="en_US.UTF-8"
export LC_NUMERIC="en_US.UTF-8"
export LC_TIME="en_US.UTF-8"
export LC_ALL="en_US.UTF-8"

# Python pip
export PIP_VIRTUALENV_BASE=$WORKON_HOME
export PIP_RESPECT_VIRTUALENV=true
# Download-cache has been deprecated and will be removed in the future.
# Pip now automatically uses and configures its cache
# export PIP_DOWNLOAD_CACHE="$HOME/.pip_download_cache"

# Python virtualenvwrapper
export WORKON_HOME="$HOME/.virtualenvs"
export PROJECT_HOME="$HOME/code"
if which virtualenvwrapper.sh > /dev/null 2>&1; then
    source $(which virtualenvwrapper.sh)
fi

# Ruby RVM
__source_if_exists "$HOME/.rvm/scripts/rvm"

if [[ -S "$SSH_AUTH_SOCK" && ! -h "$SSH_AUTH_SOCK" ]]; then
    ln -sf "$SSH_AUTH_SOCK" ~/.ssh/ssh_auth_sock;
fi
export SSH_AUTH_SOCK=~/.ssh/ssh_auth_sock;
